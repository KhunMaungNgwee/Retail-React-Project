export type LoginPayload = {
    email: string
    password: string
}

export type LoginResponse = {
    data: string
}

export type AddUserModel ={
    email:strung
    password:string
    role:string
}